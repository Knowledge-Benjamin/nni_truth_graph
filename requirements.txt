# Python 3.11+ Core Backend Dependencies
# All packages must have pre-built wheels for production stability

# Core Backend
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.4.2
python-dotenv==1.0.0
requests==2.31.0

# Database & Graph
psycopg2-binary==2.9.9
neo4j==5.21.0

# AI & NLP (wheel-distributed only)
google-genai==0.3.0
huggingface-hub==1.2.3
numpy==1.26.2
groq==0.4.1
# sentence-transformers==2.7.0 - ONLY for local mode, causes model downloads in cloud mode (disabled)

# Web Scraping & Content Processing
trafilatura==1.6.0
feedparser==6.0.10

# Utilities
python-dateutil==2.8.2

