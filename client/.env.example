# Client Environment Variables
# Copy this file to .env and fill in your actual values

# Express API server base URL — do NOT include /api suffix, that is appended automatically
# Development: http://localhost:5000
# Production:  https://your-server.onrender.com  (no port — handled by the platform)
VITE_API_URL=http://localhost:5000
