# Scripts Pipeline Dependencies - Production Ready
# Python 3.11+ only - all packages with pre-built wheels

# Core & Database
psycopg2-binary==2.9.9
neo4j==5.21.0
python-dotenv==1.0.0

# HTTP & API
requests==2.31.0

# Data Processing
pandas==2.0.3
numpy==1.26.2

# Web Scraping & Content
trafilatura==1.6.0
feedparser==6.0.10

# NLP & ML (wheel-only)
huggingface-hub==0.19.4
# sentence-transformers==2.7.0 - Only needed for local embeddings, disabled in cloud mode

# AI & LLM
groq==0.4.1
google-genai==0.3.0

# Utilities
python-dateutil==2.8.2
