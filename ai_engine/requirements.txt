# Core
python-dotenv==1.0.0
requests>=2.31.0
pydantic>=2.7.0
bcrypt>=4.0.0

# Groq LLM API
groq==1.0.0
# Google AI Studio (Gemma 4 — primary HEAVY tier)
# NOTE: google-generativeai is deprecated. Use google-genai instead.
google-genai>=0.8.0
instructor>=1.7.0           # Structured LLM outputs via Pydantic (>=1.7.0 uses google.genai, not deprecated google.generativeai)
tenacity==8.2.3             # Retry logic for API calls

# Local embeddings — disabled; all embeddings route through ai_engine.core.inference_pool
# sentence-transformers and torch are removed in production to avoid local model downloads.

# Databases
neo4j==5.21.0
psycopg2-binary==2.9.9

# Ingestion & Scraping
feedparser==6.0.10
trafilatura==1.6.0
playwright==1.42.0          # JS-rendered pages; run: playwright install chromium
cdx-toolkit>=0.9.9          # Common Crawl CDX API client with polite paging

# Task Queue
celery[sqlalchemy]==5.3.6
kagglehub

# Multimedia Engine
yt-dlp>=2024.03.10          # Autonomous video extraction
opencv-python-headless>=4.9.0  # Keyframe extraction (headless for server env)

langdetect==1.0.9
